# Requirements for core
feedparser
pillow
psycopg2
requests
sqlalchemy
nose
lxml
flask
isbnlib

# This is used by core, but only if you import integration.s3 (which we do).
tinys3

# TODO: This is only used for summary evaluation, which I think should
# only happen in the metadata wrangler, so it should be possible to move
# it out of core.
textblob

# Requirements
rdflib

